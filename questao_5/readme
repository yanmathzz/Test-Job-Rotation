Nesse código, a string é informada pelo usuário através da função input(). Em seguida, é criada uma variável string_invertida inicialmente vazia. Através de um loop for que varre a string de trás para frente, cada caractere é adicionado à variável string_invertida em ordem invertida.

Por fim, é exibida a string invertida na tela através da função print().



